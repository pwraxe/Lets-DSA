Permutation Vs TakeNotTake 

In Permutation Method We swap element twice in loop
In Take not take Method, we add element in sublist, and then remobve element from sublist and recursive call twice, after adding and after removing element from subList
